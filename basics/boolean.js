// === - equality operator
// !== - no equal operator
// < - less than operator
// > - greater than operator
// >= - greater than or equal
// <= - less than or equal

// let temp = 100;
// let freezing = temp <= 100;

// console.log(freezing);


// Challenge
    // Create an age variable and set it to your age
    // Calculate is child - if they are 7 or under
    // Calculate is senior - if they are 65 or older
    // Print is child value
    // Print is senior value

let age = 65;
let isChild = age <= 7;
let isSenior = age >= 65;

console.log(isChild);
console.log(isSenior);